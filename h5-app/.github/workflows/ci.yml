# Matoo Power H5-App · CI
# 跑后端 e2e + 前端单测 + 两端 build + OpenAPI 校验

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# 同一 PR 取消旧的运行
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================================
  # 后端：e2e + build + OpenAPI 生成验证
  # ============================================================
  api-test:
    name: API e2e + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: apps/api/package-lock.json

      - name: Install deps
        run: npm install --no-audit --no-fund --ignore-scripts

      - name: Build
        run: npm run build

      - name: Run e2e tests (30 tests)
        run: npm run test:e2e

      # 启动后端 + 验证健康（10 秒）
      - name: Boot smoke test
        run: |
          set -e
          node prisma/init-sqlite.cjs /tmp/ci.db
          DATABASE_URL=file:/tmp/ci.db node dist/src/main.js &
          SERVER_PID=$!
          sleep 3
          # SKU 不存在 → 404
          test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/sku/UNKNOWN)" = "404"
          # OpenAPI 存在
          test "$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/api-json)" = "200"
          kill $SERVER_PID 2>/dev/null || true

  # ============================================================
  # 前端：vitest + build + 类型检查
  # ============================================================
  web-test:
    name: Web tests + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: apps/web/package-lock.json

      - name: Install deps
        run: npm install --no-audit --no-fund

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests (vitest)
        run: npm test

      - name: Build (Next.js)
        run: npm run build

  # ============================================================
  # 前端 E2E：Playwright（双 webServer 真实联通：web 3100 → api 3101 → 临时 seed 库）
  # CI 走 chromium；截图断言由 SNAP = !process.env.CI 自动跳过（字体渲染跨 OS 不可靠）
  # ============================================================
  web-e2e:
    name: Web e2e (Playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install web deps
        working-directory: apps/web
        run: npm install --no-audit --no-fund

      - name: Install api deps + build (e2e 需要 api dist/src/main.js)
        working-directory: apps/api
        run: |
          npm install --no-audit --no-fund --ignore-scripts
          npm run build

      - name: Install Playwright chromium (+ 系统依赖)
        working-directory: apps/web
        run: npx playwright install --with-deps chromium

      - name: Run e2e (7 flows · workers=1)
        working-directory: apps/web
        run: npx playwright test --workers=1

  # ============================================================
  # 共享包：build + test
  # ============================================================
  shared-test:
    name: Shared package
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/shared
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: packages/shared/package-lock.json

      - name: Install + build + test
        run: |
          npm install --no-audit --no-fund
          npm run build
          npm test

  # ============================================================
  # 总结：所有 job 通过则 ✓
  # ============================================================
  all-pass:
    name: All checks
    runs-on: ubuntu-latest
    needs: [api-test, web-test, web-e2e, shared-test]
    if: always()
    steps:
      - name: Check status
        run: |
          if [ "${{ needs.api-test.result }}" != "success" ] || \
             [ "${{ needs.web-test.result }}" != "success" ] || \
             [ "${{ needs.web-e2e.result }}" != "success" ] || \
             [ "${{ needs.shared-test.result }}" != "success" ]; then
            echo "::error::One or more jobs failed"
            exit 1
          fi
          echo "✓ All CI checks passed"