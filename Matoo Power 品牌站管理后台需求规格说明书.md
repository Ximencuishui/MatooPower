# 《Matoo Power 品牌站 · 管理后台需求规格说明书》v1.0

> **文档版本**：v1.0（2026-09-20）
> **适用项目**：Matoo Power To B 品牌站（`website/`）
> **目标读者**：产品负责人 / 运维 / 内容编辑 / 前端工程师
> **设计原则**：零改造前台、文件直存、零数据库、一键启动

---

## 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-20 | 初版。基于现有静态架构 + Node.js 一站式部署设计 |

---

## 一、项目背景与目标

### 1.1 背景

Matoo Power 品牌站当前是纯静态站点（12 个 HTML + 三语 i18n + 174 张图片资产）。所有内容更新需通过修改源文件 + 重新部署，操作门槛高、响应慢、不利于非技术人员直接维护。

随着内容迭代进入常规运营期（产品文案调整、市场活动文案、Logo/场景图更新），需要一套**面向非技术运营**的内容编辑后台，让具备基础电脑操作能力的同事能够：

- 直接在浏览器中修改前端文字与图片
- 修改后前台刷新即见，无需重新部署
- 操作可追溯，避免误改

### 1.2 目标

| 维度 | 目标 |
|------|------|
| **业务** | 让非工程师可独立维护前端文案与图片素材 |
| **体验** | 零学习成本，所见即所改（前台刷新即生效） |
| **部署** | 一条命令启动，与现有 `start-preview.ps1` 同等复杂度 |
| **安全** | 单密码登录，操作日志可追溯 |
| **架构** | 不引入数据库；现有前台零改造；不动现有 i18n 与 assets 目录结构 |

### 1.3 非目标

- ❌ 多人协同编辑（v1 单管理员即可满足）
- ❌ 版本回滚与发布审批流（v1 文件直存 + 简单日志，保留 Git 即回滚）
- ❌ 在线交易、设备保修等业务功能（仍属 H5-App / 模块化商城范畴）
- ❌ 内容审核工作流
- ❌ 多语言翻译流程（AI 翻译仍走现有注入脚本路径）

---

## 二、整体架构

### 2.1 一图概览

```
┌──────────────────────────────────────────────────────────────┐
│                       Matoo Power 品牌站                        │
│                       https://matoopower.com                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────┐    ┌────────────────┐    ┌──────────────┐ │
│   │  前台用户    │    │  后台管理员     │    │  文件系统      │ │
│   │  /index.html │    │  /admin/login   │    │  /i18n/*.json│ │
│   │  /products   │◄───┤  /admin/app     │◄───┤  /assets/*   │ │
│   │  /contact    │    │                │    │  /api/data   │ │
│   └──────┬──────┘    └───────┬────────┘    └──────┬───────┘ │
│          │                   │                   │          │
│          └─────────┬─────────┴───────────────────┘          │
│                    │                                          │
│            ┌───────▼────────┐                                 │
│            │  Express 服务   │   单进程，端口 8000              │
│            │  ├─ /api/*      │   鉴权 + JSON API               │
│            │  ├─ /admin/*    │   静态托管后台                   │
│            │  └─ /*          │   静态托管前台                   │
│            └────────────────┘                                 │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 部署形态

**单一 Node.js 进程同时托管**：

| 路径前缀 | 内容 | 说明 |
|---------|------|------|
| `/api/*` | RESTful JSON API | 后端逻辑（鉴权、i18n、图片） |
| `/admin/*` | 后台静态资源 | `admin/index.html`、`admin/app.html` 等 |
| `/*` 其余 | 前台静态资源 | 现有 12 个 HTML + assets + i18n + styles + scripts |

启动命令：

```powershell
# 一条命令启动所有
.\start-admin.ps1
```

访问入口：

- 前台：`http://localhost:8000/`
- 后台：`http://localhost:8000/admin/`（自动跳转登录）
- 健康检查：`/health`

---

## 三、目录结构

新增目录（保留现有目录不动）：

```
website/
├── admin/                          ← 新增：后台前端
│   ├── index.html                  登录页
│   ├── app.html                    主工作台
│   ├── assets/
│   │   ├── admin.css               后台样式
│   │   ├── admin.js                主工作台脚本
│   │   └── login.js                登录脚本
│   └── README.md                   后台使用说明
│
├── api/                            ← 新增：后端服务
│   ├── server.js                   Express 入口
│   ├── package.json                依赖（仅 express + multer + cookie-parser）
│   ├── .env.example                环境变量样例
│   ├── routes/
│   │   ├── auth.js                 登录 / 登出 / 会话校验
│   │   ├── i18n.js                 i18n 读取 / 写入
│   │   ├── images.js               图片列表 / 上传 / 替换 / 删除
│   │   └── audit.js                操作日志查询
│   ├── lib/
│   │   ├── auth.js                 密码哈希 + 令牌签发
│   │   ├── store.js                文件读写封装（带锁）
│   │   └── logger.js               操作日志
│   └── data/                       运行时数据（被 .gitignore）
│       ├── sessions.json           当前会话
│       └── audit.log               操作日志
│
├── start-admin.ps1                 ← 新增：一键启动
│
└── [现有目录：i18n/、assets/、scripts/、styles/、pages/、12 个 HTML]
```

---

## 四、功能需求

### 4.1 登录与会话

| 项 | 规格 |
|----|------|
| 入口 | `http://localhost:8000/admin/` |
| 凭证 | 单密码（环境变量 `ADMIN_PASSWORD`，首次启动随机生成并打印） |
| 会话 | HttpOnly Cookie + 服务端 session（48 小时滑动过期） |
| 失败处理 | 5 次失败后锁定 15 分钟 |
| 退出 | 顶部导航「退出」按钮，立即失效 Cookie |

### 4.2 三语文本编辑

#### 4.2.1 编辑界面

主界面分三栏：

```
┌──────────────────────────────────────────────────────────────┐
│ 顶部：语言切换 (EN | ZH | BN)  ·  页面筛选  ·  保存状态指示    │
├────────────┬──────────────────────────────────┬──────────────┤
│            │                                  │              │
│  翻译键    │   编辑区（按选中语言）              │   实时预览    │
│  树状导航  │   <textarea> 多行编辑             │   iframe     │
│            │                                  │   前台页面    │
│            │                                  │              │
└────────────┴──────────────────────────────────┴──────────────┘
```

#### 4.2.2 操作能力

- **读取**：自动扫描 `i18n/en.json`、`zh.json`、`bn.json`，按层级展开（`nav.products`、`hero.title` 等）
- **编辑**：在 textarea 中修改，右下角显示变更标记（●）
- **保存**：
  - 单条保存（按钮）
  - 批量保存（语言维度的「保存所有变更」）
  - 自动保存（30 秒无操作触发）
- **回退**：编辑前自动备份当前值到 `i18n/.bak/`，可通过「操作日志」恢复
- **校验**：
  - JSON 解析失败时禁止保存并提示
  - 缺失翻译键标红警告
  - 多语言一致性检查（一键对比三个语言文件，找出仅存在于某一种语言的键）

#### 4.2.3 翻译键缺失检测

后台启动时扫描所有 HTML 的 `data-i18n="..."` 属性，生成「键使用清单」，与 `i18n/*.json` 做差集：

- **未翻译键**：HTML 引用但 JSON 中找不到 → 红色标记
- **未使用键**：JSON 中存在但 HTML 中未引用 → 黄色标记（可保留，不强制删除）
- 一键「跳转到未翻译键」按钮

### 4.3 图片管理

#### 4.3.1 图片列表

按目录分组（与 `assets/` 实际结构一致）：

```
assets/
├── logo.svg
├── hero/
│   ├── hero-home.jpg
│   └── hero-home@1200.jpg
├── products/
│   ├── product-power01-front.jpg
│   └── ...
└── scenarios/
    └── ...
```

列表展示：

- 缩略图（200×200 缩放）
- 文件名 + 体积 + 尺寸
- 引用页（哪些 HTML 引用了此图，自动扫描 `<img src>` 与 `srcset`）
- 操作按钮：替换 / 删除 / 复制路径

#### 4.3.2 上传 / 替换

- **拖拽上传**：支持多文件；自动按命名规则生成 `@480/@800/@1200/@1920` 变体（使用 `sharp`，需新增依赖；如环境无 `sharp`，降级为「仅原图」并提示）
- **替换**：保留原文件名，原图自动备份到 `assets/.archive/`
- **命名校验**：禁止中文/空格/特殊字符；自动建议规范名（`hero-home-2.jpg` 等）
- **格式白名单**：`.jpg`、`.png`、`.webp`、`.svg`
- **体积限制**：单文件 ≤ 10MB（生产环境可调）

#### 4.3.3 图片引用关系

后台扫描所有 HTML 中的 `<img src>` 与 `srcset`，构建「图片 → 引用页」反向索引：

- 删除图片时，若存在引用，弹出二次确认并列出受影响页面
- 替换图片时，自动同步更新 `srcset` 变体引用（同名同基础名的多尺寸）

### 4.4 操作日志

`api/data/audit.log`（追加写入，不限大小上限）记录所有变更：

```json
{
  "ts": "2026-09-20T10:32:11.234Z",
  "user": "admin",
  "action": "i18n.update",
  "target": "hero.title@zh",
  "before": "为新兴市场...",
  "after": "为全球新兴市场..."
}
```

后台「操作日志」页可：

- 按时间筛选
- 按类型筛选（i18n / image / auth）
- 导出 CSV

### 4.5 健康检查与状态

`/health` 端点返回：

```json
{
  "ok": true,
  "version": "1.0.0",
  "uptime_sec": 1234,
  "i18n_languages": ["en", "zh", "bn"],
  "image_count": 174,
  "storage": {
    "i18n_kb": 18,
    "assets_mb": 42
  }
}
```

后台首页顶部展示版本与运行时间。

---

## 五、API 设计

### 5.1 总览

| 方法 | 路径 | 鉴权 | 用途 |
|------|------|------|------|
| GET | `/health` | 否 | 健康检查 |
| POST | `/api/auth/login` | 否 | 登录 |
| POST | `/api/auth/logout` | 是 | 退出 |
| GET | `/api/auth/me` | 是 | 当前会话信息 |
| GET | `/api/i18n` | 是 | 读取所有语言文件 |
| GET | `/api/i18n/:lang` | 是 | 读取单语言 |
| PUT | `/api/i18n/:lang/:key` | 是 | 更新单条翻译 |
| GET | `/api/i18n/audit/keys` | 是 | 翻译键使用清单 |
| GET | `/api/images` | 是 | 图片列表 |
| POST | `/api/images/upload` | 是 | 上传图片（multipart） |
| POST | `/api/images/replace` | 是 | 替换图片 |
| DELETE | `/api/images/:path` | 是 | 删除图片 |
| GET | `/api/images/references/:path` | 是 | 图片引用页列表 |
| GET | `/api/audit` | 是 | 操作日志（分页） |

### 5.2 错误约定

所有错误响应：

```json
{
  "ok": false,
  "code": "INVALID_INPUT",
  "message": "Translation value cannot be empty"
}
```

常用错误码：

| code | HTTP | 含义 |
|------|------|------|
| `UNAUTHORIZED` | 401 | 未登录或会话过期 |
| `FORBIDDEN` | 403 | 权限不足 |
| `NOT_FOUND` | 404 | 资源不存在 |
| `INVALID_INPUT` | 400 | 参数不合法 |
| `STORAGE_ERROR` | 500 | 文件读写失败 |
| `RATE_LIMITED` | 429 | 请求过快 |

---

## 六、安全需求

| 项 | 措施 |
|----|------|
| **密码存储** | 仅存哈希（bcrypt 或 scrypt）于 `api/data/admin.json`，首次启动生成随机 8 位密码并控制台打印 |
| **会话** | HttpOnly + SameSite=Strict Cookie；服务端 session 存储；48 小时滑动过期 |
| **CSRF** | 所有写操作要求 `X-CSRF-Token` 头（登录后从 `/api/auth/me` 返回） |
| **路径穿越** | 图片路径统一白名单校验（拒绝 `..`、`\\`、绝对路径） |
| **上传校验** | MIME 与文件魔数双重校验；图片白名单 |
| **登录限速** | 同 IP 5 分钟内 5 次失败锁 15 分钟 |
| **HTTPS** | 生产部署建议反代（Nginx/Caddy）开启 HTTPS；后台默认仅监听 127.0.0.1 |
| **审计** | 所有写操作记录操作日志 |

---

## 七、部署与运维

### 7.1 一键启动

```powershell
# 在 website 目录下执行
.\start-admin.ps1
```

脚本逻辑：

1. 检查 Node.js ≥ 18（提示安装）
2. 若 `api/node_modules` 缺失 → `npm install --omit=dev`
4. 若 `api/data/admin.json` 不存在 → 生成随机密码并写入文件，控制台打印
5. 启动 Express 服务（端口 8000，默认绑定 `127.0.0.1`）
6. 打开浏览器访问 `http://localhost:8000/admin/`

### 7.2 与现有 `start-preview.ps1` 关系

| 场景 | 推荐脚本 |
|------|---------|
| 仅本地预览（无后台） | 现有 `start-preview.ps1`（Python/Node 静态托管） |
| 预览 + 后台管理（推荐） | 新增 `start-admin.ps1` |
| 生产部署 | 仍用 Vercel/Netlify/CF Pages 静态托管；后台**不部署**到生产 |

> **关键决策**：后台仅用于本地内容维护，生产环境无需部署。修改保存后 `git push` 即可生效。

### 7.3 配置

环境变量（`api/.env`，首次启动生成 `.env.example`）：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | 8000 | HTTP 端口 |
| `BIND` | 127.0.0.1 | 监听地址（生产可改 0.0.0.0） |
| `ADMIN_PASSWORD` | 随机生成 | 管理员密码（首次启动） |
| `SESSION_SECRET` | 随机生成 | 会话签名密钥 |
| `MAX_UPLOAD_MB` | 10 | 单文件体积上限 |
| `SHARP_ENABLED` | true | 是否启用 sharp 多尺寸生成 |

### 7.4 数据备份

- i18n 文件：纳入 Git 版本控制（已有）
- assets 原图：纳入 Git LFS 或定期打包
- 操作日志：`api/data/audit.log` 由运维定期归档
- 自动备份：编辑前自动备份到 `i18n/.bak/`（保留最近 50 个版本）

---

## 八、验收用例

| ID | 场景 | 期望 |
|----|------|------|
| AC-01 | 首次启动 | 自动生成随机密码、控制台打印、写入文件 |
| AC-02 | 错误密码登录 | 提示「密码错误」，5 次后锁定 |
| AC-03 | 修改中文 hero.title | 保存后 `i18n/zh.json` 文件变化，前台刷新即时生效 |
| AC-04 | 故意写错 JSON | 保存按钮被禁用，提示「JSON 解析失败：xxx」 |
| AC-05 | 上传新图片 | 图片出现在列表，前台 HTML 中替换 src 后可见 |
| AC-06 | 删除被引用的图片 | 弹窗列出 5 个引用页面，要求二次确认 |
| AC-07 | 翻译键缺失 | 红色标记「nav.newKey 未翻译」，一键跳转到编辑 |
| AC-08 | 多语言一致性 | 一键对比 EN/ZH/BN，列出仅在英文中存在的键 |
| AC-09 | 操作日志 | 列出最近 50 条变更，可导出 CSV |
| AC-10 | 关闭并重启服务 | 数据不丢失（仅文件存储，无数据库依赖） |
| AC-11 | 与现有 start-preview 共存 | 二者互不干扰，端口不冲突 |

---

## 九、风险与限制

| 风险 | 影响 | 缓解 |
|------|------|------|
| 直接写文件，存在并发覆盖 | 多人同时编辑同一键 | v1 单管理员；保留文件锁；后续可加乐观锁 |
| 无版本回滚 | 误改难恢复 | Git 兜底 + 自动 `.bak/` 备份 |
| sharp 依赖体积大 | 首次安装慢 | 失败时降级为原图上传，不阻塞使用 |
| 后台暴露公网 | 安全风险 | 默认仅 `127.0.0.1`；生产禁止部署 |
| 图片无 CDN 处理 | 上传图片未走 CDN | 上传后保留原图，由运营触发 `generate-srcset.ps1` 重新生成多尺寸 |

---

## 十、后续演进（v2.x 路线）

| 版本 | 范围 |
|------|------|
| v1.1 | 翻译键使用情况可视化（按 HTML 页聚合）；批量翻译导入导出 |
| v1.2 | 表单字段管理（联系表单、询盘字段在线编辑） |
| v2.0 | 多管理员 + RBAC（编辑 / 审核 / 管理员） |
| v2.1 | 草稿模式（修改不直接落盘，预览后确认） |
| v2.2 | 发布快照（修改当前自动 git commit） |
| v3.0 | 可选嵌入 SQLite，留存操作历史与版本树 |

---

## 十一、附录：关键决策记录

| # | 决策项 | 结论 | 理由 |
|---|--------|------|------|
| 1 | 后端语言 | Node.js + Express | 与现有 `npx serve` 环境一致；学习成本低 |
| 2 | 数据存储 | 文件系统直存 | 零数据库；改动可走 Git；与现有 i18n/资产一致 |
| 3 | 鉴权 | 单密码 + HttpOnly Cookie | 团队规模 1-3 人；无 LDAP/OAuth 需求 |
| 4 | 后台部署 | 不监听 | 后台是内容维护工具，不应承担访问压力 |
| 5 | 端口 | 8000 | 与现有 `start-preview.ps1` 一致 |
| 6 | 前台改造 | 零改造 | `data-i18n` 已就绪；后台直接编辑 JSON 即可 |
| 7 | 图片多尺寸 | sharp 自动生成（可降级） | 复用现有 `generate-srcset.ps1` 命名规则 |
| 8 | UI 风格 | 简洁、单栏为主 | 遵循用户界面简洁化偏好 |

---

> **下一步实施**：见 `admin/README.md` 与本仓库新增的 `api/`、`admin/`、`start-admin.ps1`。